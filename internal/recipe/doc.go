// Package recipe parses, validates and canonicalises the recipe.
//
// The recipe is the single source of configuration shared by the command line
// and the window. Validation reports every problem at once, never the first
// one it meets.
package recipe
