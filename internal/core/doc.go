// Package core holds the arithmetic every format depends on - exact size
// targeting, seed derivation, name templates, hashing and label composition.
//
// It knows nothing about any file format. That is deliberate - the size
// arithmetic must not learn about PDF.
package core
