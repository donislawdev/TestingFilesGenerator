// Package zip generates ZIP archives, including archives that pull in other generators.
//
// No generator is implemented yet. This package exists so the layout and the
// guard tests are in place before the first format is written.
package zip
